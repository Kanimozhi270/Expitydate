<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpiryDateManager"
        tools:targetApi="31">
        <activity
            android:name=".Activity.Expiry_FullView"
            android:exported="false" />

        <receiver
            android:name=".Notification.NotificationReceiver"
            android:exported="false" />

        <activity
            android:name=".Activity.ItemNamesList"
            android:exported="false" />
        <activity
            android:name=".Activity.Category"
            android:exported="false" />
        <activity
            android:name=".Activity.AddItemActivity"
            android:exported="false" />
        <activity
            android:name=".Activity.ExpiryDate_Homepage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>